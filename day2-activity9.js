//Create 2 variables called num1 and num2. Create an if statement that checks if the result of the sum is even. If it is return the number, otherwise return the numbers multiplied together

let num1 = 7
let num2 = 2
let sumOfNum = num1 + num2;
let productOfNum = num1 * num2;

(sumOfNum % 2 == 0) ? console.log(`${sumOfNum}`) : console.log(`${productOfNum}`);

